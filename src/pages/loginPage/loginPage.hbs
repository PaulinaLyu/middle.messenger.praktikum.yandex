{{#> Card }}
  <form class="login-page">
    <div class="login-page__body">
      {{#if isRegistration}}
        {{> Title title="Регистрация" }}
        {{> InputField className="login-page__input" title="Почта" name="email" }}
        {{> InputField className="login-page__input" title="Логин" name="login" }}
        {{> InputField className="login-page__input" title="Имя" name="name" }}
        {{> InputField className="login-page__input" title="Фамилия" name="first_name" }}
        {{> InputField className="login-page__input" title="Телефон" name="phone" }}
        {{> InputField className="login-page__input" title="Пароль" name="password" type="password" }}
        {{> InputField className="login-page__input" title="Пароль (еще раз)" name="password" type="password" }}
      {{else}}
        {{> Title  title="Вход" }}
        {{> InputField className="login-page__input" title="Логин" name="login" }}
        {{> InputField className="login-page__input" title="Пароль" name="password" type="password" }}
      {{/if}}
    </div>
    <div>
      {{#> Button page="chat" type="button" }}
          <span>{{#if isRegistration}}Зарегистрироваться{{else}}Авторизоваться{{/if}}</span>
      {{/ Button }}
      {{#if isRegistration}}
          {{> Link page="login" text='Войти' }}
      {{else}}
          {{> Link page="login-registration" text='Нет аккаунта?' }}
      {{/if}}
    </div>
  </form>

{{/ Card }}